package TH2;

public class Task1_4 {
	public static int MoveTower(int disk, int source, int dest, int square) {
		if(disk == 0 ) {
//			move disk from source to dest
		}else {
//			moveTower(disk - 1, source, square, dest)
//			move disk from source to dest
//			moveTower(disk -1, sqare, dest, source)
		}
		return 0;
	}
}
