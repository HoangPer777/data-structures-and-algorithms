package medium;

public class _7_reverse {
	public int reverse(int x) {
	       if((x <= Math.pow(-2,31)) || (x >= Math.pow(2, 31) -1)) {
	           return 0;
	       }else{
	    	   
	    	   return x;
	       }
		
	    }
}
